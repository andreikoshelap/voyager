# local: long polling instead of webhook, convenient for debugging without an HTTPS tunnel.
# Before starting, remove the webhook once:
#   curl "https://api.telegram.org/bot<TOKEN>/deleteWebhook"
voyage:
  grace-period: PT2M
  alert-delay: PT1M
